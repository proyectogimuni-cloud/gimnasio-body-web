/**
 * Enrutador Main para modulos dentro de la aplicación, una vez el usuario realiza la autenticación
 */
export enum MainRoutes {
    /** Ruta al módulo de registro de asistencia */
    REGISTRO_ASISTENCIA = 'registro-asistencia',
    /** Ruta al módulo de panel de control */
    PANEL_DE_CONTROL = 'panel-control',
}